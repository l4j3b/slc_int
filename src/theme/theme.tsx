import { ThemeConfig } from "antd";

export const colors = {
  primary: "#337866",
  secondary: "#265b4e",
};

const theme: ThemeConfig = {
  token: {
    colorPrimary: colors.primary,
  },
};

export default theme;
